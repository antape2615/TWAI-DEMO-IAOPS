// import React from 'react';

function AppDebug() {
    return (
        <div style={{ padding: '50px', textAlign: 'center' }}>
            <h1 style={{ fontSize: '30px', color: 'green' }}>
                DEBUG MODE: FRONTEND IS WORKING
            </h1>
            <p>If you can see this, the build system is fine, and the error is in one of the components.</p>
        </div>
    );
}

export default AppDebug;
